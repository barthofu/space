export const physicsConfig = {

    speed: {        
        acceleration: 5,
        deceleration: 0.995, // don't touch
    },

    rotation: {
        acceleration: 4,
        deceleration: 0.98, // don't touch
    },

    collisions: {
        knockbackStrength: 0.8, // must be less than 1
    }

}